// Antithesis script to null_cancelling_movement.cfg
// Resets movement to default wasd

bind "a" "+moveleft"		// Strafe Left
bind "d" "+moveright"		// Strafe Right
bind "s" "+back"			// Move Backward
bind "w" "+forward"			// Move Forward

echo "Config: Movement key functions returned to default!"